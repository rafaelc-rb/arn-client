/// ViewModel responsável pela lógica de navegação do WebView.
/// Segue o padrão MVVM para separação de responsabilidades.
class WebViewViewModel {
  /// URL que será exibida no WebView.
  final String url;

  /// Construtor do ViewModel.
  WebViewViewModel(this.url);
}
